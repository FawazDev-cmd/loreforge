# LoreForge frontend environment template.
# Copy to .env.local for local frontend development.
# Do not put Gemini keys, database URLs, or server API keys in frontend env files.

VITE_API_BASE_URL=http://127.0.0.1:8000
